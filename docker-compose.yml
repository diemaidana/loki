services:
  angular-loki:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: angular-loki
    ports:
      - "4200:4200"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - json-server
    networks:
      - loki-network

  json-server:
    build:
      context: ./json-server
      dockerfile: Dockerfile
    container_name: json-server
    ports:
      - "3000:3000"
    volumes:
      - ./json-server:/app
    networks:
      - loki-network

networks:
  loki-network:
    driver: bridge
